// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyA3BKX3UlB-Hm001pSc5mRfuAWa1WWOkWk",
    authDomain: "fireshipsortable.firebaseapp.com",
    databaseURL: "https://fireshipsortable.firebaseio.com",
    projectId: "fireshipsortable",
    storageBucket: "fireshipsortable.appspot.com",
    messagingSenderId: "30606889937",
    appId: "1:30606889937:web:2e8fffbf8f96a7eb1ec6f0"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
